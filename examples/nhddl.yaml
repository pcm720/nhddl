#video: ntsc # supported video modes: ntsc, pal, 480p, 576p, 720p, 1080i (720p/1080i need component cables)
mode: ata # supported modes: ata, mx4sio, udpfs, usb, ilink, mmce, hdl. If not present or commented out, all devices will be used to search for ISO files
mode: mmce # multiple modes are supported via multiple mode strings
#udpfs_ip: 192.168.1.6 # PS2 IP address for UDPFS mode (commented out)
